<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Tutrix</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h2>Welcome, <%= user.name %>!</h2>

        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Graduation Year:</strong> <%= profile.graduationYear %></p>
        <p><strong>Major:</strong> <%= profile.major %></p>
        <p><strong>Interests:</strong> <%= profile.interests.join(', ') %></p>
        <p><strong>Completed Sessions:</strong> <%= profile.completedSessions %></p>
        <p><strong>Upcoming Lessons:</strong> <%= profile.upcomingLessons %></p>

        <a href="/tasks" class="btn">Go to Tasks</a>
    </div>
</body>
</html>
