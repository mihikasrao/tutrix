<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Student Dashboard</title>
  <link rel="stylesheet" type="text/css" href="/style.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    h1 {
      margin-bottom: 20px;
      color: #9370DB; /* Lavender color for Tutrix */
    }

    .action-buttons {
      display: flex;
      gap: 15px;
      margin-bottom: 20px;
    }

    button {
      background-color: #9370DB; /* Lavender color for Tutrix */
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #7A5CBA; /* Darker lavender on hover */
    }

    a {
      color: #9370DB; /* Lavender color for Tutrix */
      text-decoration: none;
      font-size: 16px;
      transition: color 0.3s ease;
    }

    a:hover {
      color: #7A5CBA; /* Darker lavender on hover */
    }
  </style>
</head>
<body>
  <h1>Welcome Student, <%= user.name %>!</h1>
  <div class="action-buttons">
    <button onclick="window.location.href='/tutor-availability'">View Tutor Availability</button>
    <button onclick="window.location.href='/profile'">Back to Profile</button>
  </div>
  <a href="/logout">Logout</a>
</body>
</html>
