<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tutor Login - Tutrix</title>
    <style>
        /* Global Styles */
        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: #f7f3fc; /* Soft lavender background */
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        h2 {
            color: #6a0dad; /* Rich lavender heading */
            text-align: center;
            margin-bottom: 20px;
        }

        /* Form Container */
        .container {
            background: #ffffff; /* White card for contrast */
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 90%;
            max-width: 400px;
            padding: 20px;
        }

        /* Error Message */
        .error-message {
            color: #f44336; /* Red for errors */
            text-align: center;
            margin-bottom: 10px;
            font-size: 14px;
        }

        /* Form Fields */
        input[type=email], input[type=password] {
            width: 100%;
            padding: 12px 20px;
            margin: 8px 0;
            border: 1px solid #d3cce3; /* Soft border */
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 16px;
        }

        input[type=email]:focus, input[type=password]:focus {
            outline: none;
            border-color: #6a0dad; /* Lavender focus effect */
            box-shadow: 0 0 5px rgba(106, 13, 173, 0.4);
        }

        /* Buttons */
        button {
            background-color: #6a0dad; /* Lavender button */
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }

        button:hover {
            background-color: #8a4eaf; /* Darker lavender on hover */
            box-shadow: 0 4px 8px rgba(138, 78, 175, 0.3);
        }

        /* Remember Me Checkbox */
        label {
            display: block;
            font-size: 14px;
            color: #4b2a69; /* Deep lavender for labels */
            margin: 5px 0;
        }

        input[type=checkbox] {
            margin-right: 8px;
        }

        /* Media Queries for Mobile */
        @media screen and (max-width: 480px) {
            .container {
                width: 100%;
                margin: 0 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Tutor Login</h2>

        <% if (typeof error !== 'undefined') { %>
            <p class="error-message"><%= error %></p>
        <% } %>

        <form action="/tutor-login" method="POST">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" placeholder="Enter your email" required>

            <label for="password">Password:</label>
            <input type="password" id="password" name="password" placeholder="Enter your password" required>

            <button type="submit">Login</button>
            <label>
              <input type="checkbox" checked="checked" name="remember"> Remember me
            </label>
        </form>
    </div>
</body>
</html>
